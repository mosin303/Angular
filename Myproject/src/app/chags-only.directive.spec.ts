import { ChagsOnlyDirective } from './chags-only.directive';

describe('ChagsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ChagsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
